export const songTypeList = state => state.songTypeList

export const songTypeName = state => state.songTypeName

export const songsList = state => state.songsList

export const classifyList = state => state.classifyList

export const playing = state => state.playing

export const mode = state => state.mode

export const user = state => state.user

export const movie = state => state.movie

export const searchHistory = state => state.searchHistory

export const playHistory = state => state.playHistory

export const favoriteList = state => state.favoriteList
